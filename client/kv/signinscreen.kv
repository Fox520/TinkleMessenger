<SignInScreen>:
    GridLayout:
        rows: 2
        MDToolbar:
            id: toolbar
            title: "Sign in"
            md_bg_color: app.theme_cls.primary_color
            background_palette: 'Primary'
            background_hue: '500'
            elevation: 10
            right_action_items: [['dots-vertical', lambda x: root.show_bottom_sheet()]]

        GridLayout:
            rows: 3
            orientation: 'vertical'
            padding: [10,50,10,50]
            BoxLayout:
                orientation: "vertical"
                AsyncImage:
                    halign: "center"
                    source: "img/tinkle_logo.png"
                MDLabel:
                    font_name: "Cursive"
                    text: "Welcome"
                    halign: "center"
                    font_size: sp(50)
            GridLayout:
                cols: 1
                orientation: "vertical"
                padding: [20,0,20,0]
                spacing: 10
                MDTextField:
                    id: alias
                    hint_text: "Username"
                    max_text_length: 12
                    multiline: False
                    write_tab: False

                MDRaisedButton:

                    text:"Proceed"
                    on_release:
                        prg_spin.start_spinning()
                        Clock.schedule_once(lambda dt: root._signin(), 1.2)


            GridLayout:
                rows:4
                cols:3
                Widget:
                Widget:
                Widget:
                Widget:

                BoxLayout:
                    orientation: 'vertical'

                    BoxLayout:

                        ProgressSpinner:
                            auto_start: False
                            id:prg_spin
                            speed: 2
                            color: get_color_from_hex("#4285f4")
                            stroke_width: 1.6
                MDRaisedButton:
                    text:"Register"
                    on_release:
                        root.manager.current = "registration_screen"

                MDLabel:
                    text: ""